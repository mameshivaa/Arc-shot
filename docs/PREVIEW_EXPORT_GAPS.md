# Preview / Export Visual Gap Inventory

Purpose: inventory the current visual parity between editor preview rendering and exported video rendering, updated as M2 preview/export parity tasks land.

Date: 2026-06-20

M2 closeout status: complete. This document is now the carry-forward risk register for preview-vs-export rendered raster parity work, not a blocker for starting M3.

| Feature | Preview implementation | Export implementation | Status | Risk | Owner task |
| --- | --- | --- | --- | --- | --- |
| Zoom | `Features/Editor/EditorView.swift` `StyledVideoPreview.activePreviewZoomState`, `EditorPreviewZoomResolver`, `activePreviewTransform`, and `EditorPreviewLayout.previewZoomTransform` apply source-time zoom and live edit ranges to the `EditorPlayerLayer`. `Features/Editor/EditorLayout.swift` owns preview point/rect transforms. `ArcShotTests/ArcShotTests.swift` now runs the preview zoom resolver and compares target-region preview geometry against export zoom transforms at start, ramp boundary, hold, out boundary, and end timing samples. | `Features/Export/ArcShotCompositionInstruction.swift` `ZoomState`, `interpolatedZoomTransform`, and `zoomFrameState` apply per-frame zoom; `Features/Export/ArcShotCompositionInstructionBuilder.swift` segments instructions around zoom boundaries. `ArcShotTests/ArcShotTests.swift` also covers the export 0.12s previous-state ramp used at instruction segment boundaries. | Partial | Target-region timing/geometry samples are now covered through the real preview resolver plus layout helpers against export zoom transforms, and export segment-boundary ramp behavior is unit-covered. Remaining risk is full rendered preview-vs-export raster parity plus mid-ramp behavior: preview derives transform from visible rect/player-layer animation while export applies deterministic per-frame zoom-state interpolation. | M2-02 |
| Cursor | `Features/Editor/EditorView.swift` `StyledVideoPreview.cursorPoint`, `previewCursor`, and `CursorPreviewView` render cursor overlays in SwiftUI using `CursorRenderer.Style`; preview maps source coordinates through `EditorPreviewLayout.cursorPoint` plus active zoom transform. `ArcShotTests/ArcShotTests.swift` now compares preview cursor coordinates against the export source transform for staged and non-staged output, including active target-region zoom. | `Features/Export/ArcShotVideoCompositor.swift` `cursorDrawState` and `drawCPUOverlays` render cursor through `Features/Export/CursorRenderer.swift`; cursor samples are prepared in `Features/Export/Exporter.swift` `mapCursorSamplesForTimeline` / `shiftCursorSamplesForCompositionExport`, then export-only sample densification feeds `ArcShotCompositionInstructionBuilder` via `densifyLinearCursorSamples`. `ArcShotTests/ExportSmokeTests.swift` now samples an exact exported middle frame inside a short zoom instruction segment to prove sparse start/end cursor samples are densified into the segment and render as an interpolated middle-position dot. | Partial | Preview and export cursor coordinate mapping is now covered for staged/non-staged output and active zoom without broadening private compositor APIs, and export rendered pixel coverage now proves sparse-sample densification reaches an instruction-segmented MP4. Remaining risk is preview-vs-export rendered raster parity because preview draws SwiftUI `CursorPreviewView` while export draws via CoreGraphics in `CursorRenderer`. | M2-03 |
| Click pulse | `Features/Editor/EditorView.swift` `EditorPreviewClickPulse`, `activeClickCue`, and `previewClickCue` use `CursorRenderer.clickPulseProgress` so preview pulse timing is post-click only. | `Features/Export/Exporter.swift` `clickCuesFromInputEvents`, `mapClickCuesForTimeline`, and `shiftClickCuesForCompositionExport` feed cues; `Features/Export/ArcShotVideoCompositor.swift` uses `CursorRenderer.clickPulseProgress`; `Features/Export/CursorRenderer.swift` pulses after cue time for 0.22s. `ArcShotTests/ExportSmokeTests.swift` now samples an exact post-click exported frame to prove the rendered pulse brightens pixels away from the cursor dot. | Partial | The pre-click pulse mismatch is fixed and covered by tests, and export rendered pixel coverage now proves the CoreGraphics pulse appears after a cue. Remaining risk is preview-vs-export raster parity: preview draws SwiftUI `CursorPreviewView`, while export draws via CoreGraphics in `CursorRenderer`. | M2-04 |
| Keyboard shortcut overlay | `Features/Editor/EditorView.swift` `EditorPreviewKeyboardShortcut`, `activeShortcutOverlay`, and `previewShortcut` now filter to modifier `keyDown` events, use a 1.1s lifetime, and place the overlay in the full stage preview at normalized rect `(0.34, 0.06, 0.32, 0.075)`. | `Features/Export/Exporter.swift` `keyboardShortcutOverlays` converts keyDown input events with command/option/control modifiers into `TextOverlayAnnotation`s lasting 1.1s at normalized rect `(0.34, 0.06, 0.32, 0.075)`; `Features/Export/ArcShotVideoCompositor.swift` `drawTextOverlay` maps top-left normalized text rects into CGContext render space before CoreText drawing. `ArcShotTests/ExportSmokeTests.swift` now samples an exact exported frame to prove shortcut text renders bright pixels in the normalized overlay region. | Partial | Filtering, lifetime, normalized output placement, and exported text pixels are now covered. Remaining risk is preview-vs-export raster/style parity: preview uses SwiftUI text/capsule rendering while export uses CoreText text overlay drawing with a rectangular rounded background and 0.05s fade. | M2-05 |
| Stage background / rounded card | `Features/Editor/EditorView.swift` `stagePreview`, `floatingVideo`, and `backgroundView` use `RecordingProject.ExportVisualSettings`, `ArcShotRenderGeometry.make`, SwiftUI gradients/material blur, rounded clipping, and optional shadow. `Features/Editor/EditorPreviewTimelineSplit.swift` wraps the preview/timeline split in a material rounded panel. | `Features/Export/ArcShotCompositionInstructionBuilder.swift` builds `StageConfig` with display-capture gating. `Features/Export/ArcShotVideoCompositor.swift` `applyStageLayout`, `makeStageBackgroundImage`, `stageShadowImage`, and `clippedImage` apply background raster gradients/blur, non-window card shadow, and rounded clipping. `ArcShotTests/ExportSmokeTests.swift` samples exported pixels to prove the non-window shadow darkens only the card-adjacent band, window captures suppress that extra shadow, gradient start/end colors survive export, and rounded card corners reveal the gradient instead of black source pixels. | Partial | Preview and export share `ArcShotRenderGeometry` and export visual settings, both suppress the extra card shadow for window captures, and export has rendered fixture coverage for non-window shadow, window shadow suppression, gradient background, and rounded clipping. Remaining risk is full raster parity: preview uses SwiftUI/material shadow and gradients while export uses CoreImage/raster compositing, so preview-vs-export screenshot comparison is still missing. | M2-06 |
| Masks blur/highlight | `Features/Editor/EditorView.swift` `activeMasks`, `EditorPreviewMaskPresentation`, `previewMask`, and `MaskRegionOverlay` show active/editable mask rectangles. Preview now uses export-like fade envelope/active opacity, shared mask geometry, and closer highlight constants: radius `12`, fill `0.12`, stroke `0.86`, line width `4`. Blur masks derive their style from a named `blurProxyStrength` and use a stronger regular material proxy with denser fill/stroke styling. This remains a preview-only approximation rather than real-time Gaussian blur. | `Features/Export/ArcShotVideoCompositor.swift` `applyBlurMasks` applies CoreImage Gaussian blur to blur regions; `drawVisualMask` draws yellow highlight fill/stroke with overlay fades. Geometry helpers live in `ExportVideoGeometry.maskSourceRect` / `maskRenderRect`. `ArcShotTests/ExportSmokeTests.swift` now samples exact exported frames to prove highlight mask fill/stroke pixels render over source video and blur masks reduce local stripe contrast in the masked region without blurring a reference or mirrored region. | Partial | Highlight timing, geometry, exact highlight style constants, blur proxy style constants, exported highlight pixels, and exported blur mask pixels are now covered. Blur preview is closer than the old lightweight proxy, but it is still not exact Gaussian blur and still lacks rendered preview-vs-export raster parity for blur intensity. | M2-07 |
| Captions/text overlays | `Features/Editor/EditorView.swift` `EditorPreviewCaptionPresentation`, `activeCaptionOverlays`, and `previewCaption` now render active source-duration-sanitized `project.textOverlayAnnotations + CaptionTrack.asTextOverlays()` on the full stage preview, preserve multiple active overlays, use unique SwiftUI render IDs for duplicate source IDs, and use the export normalized rect/font assumptions, 0.05s fade envelope, black background alpha `0.35`, corner radius `6`, and text inset assumptions. | `Features/Export/ArcShotVideoCompositor.swift` `drawTextOverlay` renders `TextOverlayAnnotation` with CoreText, Helvetica, normalized rect, 0.05s fade, fixed black background alpha. `Features/Export/Exporter.swift` maps and sanitizes text overlays for timeline clips. `ArcShotTests/ExportSmokeTests.swift` now samples exact exported frames to prove a manual wrapped text overlay renders bright pixels on multiple lines and darkens source pixels behind its rounded background, and that `CaptionTrack.asTextOverlays()` text reaches export as rendered pixels. | Partial | Timing, normalized placement, manual text overlay inclusion, captionTrack-derived text overlay inclusion, source-time sanitization, duplicate-ID-safe multiple active text overlays, font size, background alpha, corner radius, inset assumptions, exported wrapped-text/background pixels, and exported captionTrack text pixels are covered. Remaining risk is preview-vs-export raster parity: preview uses SwiftUI text layout while export uses CoreText, so exact line breaks and glyph rasterization can still differ. | M2-08 |
| Fade | `Features/Editor/EditorView.swift` `EditorPreviewFade.opacity` mirrors intro/outro timing semantics and `stagePreview` applies a full-preview black overlay from `RecordingProject.StyleSettings`. | `Features/Export/ArcShotCompositionInstructionBuilder.swift` creates `FadeConfig` from `styleSettings`; `Features/Export/ArcShotVideoCompositor.swift` `fadeOpacity` and `drawCPUOverlays` draw full-frame black intro/outro fade. `ArcShotTests/ExportSmokeTests.swift` now samples exact exported frames to prove intro/outro fade darkens a bright fixture while a middle frame outside fade remains bright. | Partial | Preview and export use the same timing semantics, and export rendered pixel coverage now proves the full-frame fade reaches the MP4. Remaining risk is preview-vs-export raster parity because preview uses SwiftUI compositing while export uses CPU/CoreGraphics drawing. | M2-09 |
| Camera PiP | `Features/Editor/EditorView.swift` `activeCameraSegment`, `CameraPiPPlayerView`, `EditorPreviewCameraPresentation`, and `previewCamera` now render the secondary recording media inside the PiP/fullscreen camera region when available, using aspect-fill playback, source-time seeking, timeline speed-rate playback, mirroring, rounded clipping, tested placement helpers, and zoom-shrink behavior. It falls back to the previous `video.fill` placeholder only when no secondary media URL is available. | `Features/Export/Exporter.swift` `effectivePIPAttachment`, `PIPTimelineSourceMapper`, `addPIPVideoTrack`, and `pipVideoAspectFillTransform` add the actual secondary video track, align PiP source windows to timeline clips/trim/speed, and compute placement/aspect/mirroring; `Features/Export/ArcShotCompositionInstructionBuilder.swift` emits matching PiP clip rect/radius metadata; `Features/Export/ArcShotVideoCompositor.swift` applies rounded clipping before compositing PiP. `Features/Export/ExportMixedCompositionBuilder.swift` also aligns PiP to single-asset trim windows. `ArcShotTests/ExportSmokeTests.swift` now renders a black main source plus asymmetric, banded, and temporal camera sources to prove the exported PiP appears at the expected rect, mirrors left/right color placement, keeps an extreme rounded corner clipped to the black main source, aspect-fills by cropping source top/bottom bands in a wide PiP, advances from early red camera frames to late green camera frames over the export, and reaches the expected green source frames under a 2x timeline-speed mapping. | Partial | Preview now shows actual camera footage instead of a placeholder, timeline/trim/speed drift is covered by unit/export-smoke tests, export rounded clipping follows the same derived radius as preview, and export rendered pixel coverage now proves PiP placement, mirroring, rounded clipping, aspect-fill crop behavior, playback changing over time, and short timeline-speed sync reach the MP4. Remaining risk is preview-vs-export raster parity plus longer-duration playback sync beyond the short exact-frame smoke fixtures. | M2-10 |

## Supporting Model / Test References

- `Domain/RecordingProject.swift` defines the shared data model for `ZoomSegment`, `ZoomKeyframe`, `CursorSample`, `InputEvent.shortcutDisplayText`, `CaptionTrack`, `VisualMask`, `CameraLayoutSegment`, `CursorVisualSettings`, `CursorClickCue`, `SecondaryRecordingAttachment`, `ExportVisualSettings`, and `StyleSettings`.
- `ArcShotTests/ArcShotTests.swift` has unit coverage for preview/export geometry helpers, zoom transforms, visual mask geometry, keyboard shortcut display text, caption/mask sanitization, camera layout sanitization, and timeline effect selection.
- `ArcShotTests/ExportSmokeTests.swift` exists and covers export smoke paths including staged export, background music, preview composition audio, and an overlay export with cursor, click cue, zoom, caption, mask, camera PiP, and keyboard shortcut settings.
- `ArcShotTests/ExportSmokeIntegrationTests.swift` was not present in the inspected file list; the available export integration-style coverage is in `ArcShotTests/ExportSmokeTests.swift`.

## Prioritized Follow-up Tasks

1. M2-02: Extend zoom parity from geometry/timing unit coverage to rendered preview-vs-export raster checks, including live playback animation behavior around instruction boundaries.
2. M2-03: Extend cursor parity from coordinate fixtures and export sparse-sample densification pixels to rendered preview-vs-export raster checks.
3. M2-04: Extend click pulse coverage from the export rendered pixel fixture to a preview-vs-export raster comparison.
4. M2-05: Add preview-vs-export screenshot comparison for keyboard shortcut overlay style/fade parity.
5. M2-06: Add preview-vs-export screenshot comparison for stage card background, shadow, and rounded clipping parity.
6. M2-07: Add preview-vs-export raster comparison for blur intensity and evaluate a real Gaussian preview path if the named material proxy is still visibly insufficient.
7. M2-08: Extend wrapped text coverage from the export rendered pixel fixture to a preview-vs-export screenshot comparison for exact caption line breaks and glyph raster parity.
8. M2-09: Extend export-rendered fade coverage to a preview-vs-export screenshot comparison for intro/outro fade opacity parity.
9. M2-10: Extend camera PiP coverage from export-rendered placement/mirroring/rounded clipping/aspect-fill crop/playback-over-time/timeline-speed checks to long-duration playback sync and preview-vs-export raster comparison.
